# CLI To-Do List App 📝
A functional task manager built with Python.

### 🚀 Key Features:
- **CRUD Operations:** Ability to Add (Create), View (Read), and Remove (Delete) tasks.
- **Data Structures:** Mastered Python **Lists** to store and manipulate data dynamically.
- **User Interface:** Built an infinite loop menu (`while True`) for continuous user interaction.
